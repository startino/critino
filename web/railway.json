{
	"$schema": "https://railway.app/railway.schema.json",
	"build": {
		"builder": "NIXPACKS",
		"nixpacksPlan": {
			"providers": ["python"],
			"phases": {
				"setup": {
					"nixPkgs": ["...", "nodePackages.pnpm"]
				}
			}
		},
		"buildCommand": "pnpm i --frozen-lockfile --force && pnpm run preview"
	},
	"deploy": {
		"startCommand": "pnpm run preview",
		"restartPolicyType": "ON_FAILURE",
		"restartPolicyMaxRetries": 10
	}
}
